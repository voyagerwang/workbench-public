{
  "name": "workbench",
  "private": true,
  "version": "0.1.0",
  "description": "YZ工作台（本地单用户）",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run build --workspace web && npm run dev --workspace server",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "start": "npm run build -w web && npm run build -w server && npm run start --workspace server",
    "check": "npm run check --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  }
}
